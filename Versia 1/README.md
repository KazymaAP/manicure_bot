# 💅 Manicure Bot — Telegram-бот для записи на маникюр

Профессиональный Telegram-бот для онлайн-записи на маникюр с поддержкой администрирования, расписания, напоминаний, бэкапов и мониторинга.

## Возможности

### Для клиентов
- 📅 Запись на услугу через интерактивный календарь
- 🔄 Перенос записи на другое время
- 📋 Просмотр активных записей
- 🔔 Напоминания за 24 часа, 2 часа и 1 час до визита
- 🕐 Лист ожидания с уведомлением при отмене
- 💰 Просмотр прайс-листа и расписания
- 📤 Поделиться ботом
- 🔕 Управление уведомлениями

### Для администратора
- 📊 Дашборд с актуальной статистикой
- 📋 Фильтрация записей (сегодня / неделя / все активные)
- ✅ Отметка «клиент пришёл» с благодарностью
- 🗓 Управление расписанием и временными слотами
- 📝 Шаблоны рабочих дней
- 🚫 Чёрный список клиентов
- 📣 Массовая рассылка всем пользователям
- 💾 Автоматические бэкапы БД с ротацией
- 📦 Архивирование старых записей
- 🔍 Поиск клиентов

### Технические возможности
- 🏥 HTTP health-check сервер (`/health`, `/metrics`)
- 📈 Ежедневный дайджест статистики
- ⚡ Rate limiting (защита от флуда)
- 🔒 Защита токена бота через SecretStr
- 🌍 Поддержка любого часового пояса

## Быстрый старт

### 1. Клонирование и настройка

```bash
git clone https://github.com/KazymaAP/manicure_bot.git
cd manicure_bot
cp .env.example .env
```

### 2. Настройка `.env`

```env
# Обязательные
BOT_TOKEN=1234567890:ABCDefGhIJKlmnoPQRStuvwXYZ
ADMIN_IDS=123456789

# Важные
TIMEZONE=Europe/Moscow
REMINDER_HOURS_BEFORE=24

# Услуги и прайс
SERVICES={"Маникюр": {"price": 1200, "duration": 60}, "Педикюр": {"price": 2000, "duration": 90}}
```

### 3. Запуск

#### Через Docker (рекомендуется)
```bash
docker-compose up -d
```

#### Локально
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate  # Windows
pip install -r requirements.txt
python main.py
```

## Структура проекта

```
manicure_bot/
├── src/
│   ├── application/        # Бизнес-логика
│   │   ├── dto/            # Data Transfer Objects
│   │   ├── services/       # Сервисы приложения
│   │   └── use_cases/
│   ├── config/             # Конфигурация и DI
│   ├── domain/             # Доменные модели и исключения
│   │   ├── enums/
│   │   ├── exceptions/
│   │   └── models/
│   ├── infrastructure/     # Инфраструктурный слой
│   │   ├── database/       # DatabaseManager (SQLite + WAL)
│   │   ├── http/           # Health-check сервер
│   │   └── repositories/  # Репозитории
│   └── presentation/       # Telegram handlers, keyboards
│       ├── formatters/
│       ├── handlers/
│       ├── keyboards/
│       └── middlewares/
├── migrations/             # SQL-миграции
├── tests/                  # Unit-тесты
│   └── unit/
├── .env.example            # Шаблон конфигурации
├── Dockerfile
├── docker-compose.yml
├── main.py                 # Точка входа
└── requirements.txt
```

## Конфигурация

Все настройки задаются через переменные окружения / `.env` файл.

| Переменная | Обязательная | По умолчанию | Описание |
|---|---|---|---|
| `BOT_TOKEN` | ✅ | — | Токен от @BotFather |
| `ADMIN_IDS` | ✅ | — | Telegram ID администраторов (через запятую) |
| `TIMEZONE` | — | `UTC` | Часовой пояс (`Europe/Moscow`) |
| `DB_PATH` | — | `data/manicure_bot.db` | Путь к SQLite |
| `REMINDER_HOURS_BEFORE` | — | `24` | За сколько часов напоминать |
| `MAX_APPOINTMENTS_PER_USER` | — | `1` | Макс. активных записей у клиента |
| `SCHEDULE_DAYS_AHEAD` | — | `30` | Горизонт расписания (дней) |
| `REDIS_URL` | — | — | Redis для FSM (рекомендуется в prod) |
| `HEALTH_PORT` | — | `8080` | Порт health-check сервера |
| `METRICS_TOKEN` | — | — | Bearer-токен для `/metrics` |

## Команды и кнопки

### Пользователь (клиент)

| Команда / кнопка | Описание |
|---|---|
| `/start` | Приветствие и главное меню |
| `/help` | Справка по использованию бота |
| `/cancel` | Отмена текущего действия / возврат в меню |
| `/mybookings` | Список активных записей (кнопка «📋 Мои записи») |
| `/slots` | Расписание свободных слотов (кнопка «📆 Расписание») |
| `📅 Записаться` | Начало процесса записи (выбор услуги → дата → время → данные) |
| `📋 Мои записи` | Просмотр и управление активными записями |
| `💰 Цены` | Прайс-лист на услуги |
| `📞 Связаться с мастером` | Контактная информация мастера |
| `📆 Расписание` | Просмотр ближайших свободных дней |
| `🔔 Уведомления` | Управление напоминаниями (за 24 ч / 2 ч / 1 ч) |
| `📤 Поделиться` | Поделиться ботом с другими |
| `🏠 Главное меню` | Возврат в главное меню |

### Администратор

| Команда / кнопка | Описание |
|---|---|
| `/admin` | Открытие панели администратора |
| `📅 Сегодня` | Записи на сегодня + отметка «пришла» / отмена |
| `📋 Все записи` | Все записи с фильтром (сегодня / неделя / активные) |
| `🗓 Расписание` | Управление расписанием, слотами, шаблонами |
| `👥 Клиенты` | Поиск клиентов, история, написать клиенту |
| `⚙️ Настройки` | Имя мастера, приветствие, фото, услуги, интервал, напоминание |
| `🚫 Чёрный список` | Блокировка / разблокировка пользователей |
| `📊 Статистика` | Итоговая статистика + выручка за день/неделю/месяц |
| `📢 Рассылка` | Массовая рассылка всем пользователям бота |
| `⬇️ Экспорт CSV` | Экспорт всех записей в CSV-файл |
| `📆 Ближайшие записи` | Быстрый просмотр текущих активных записей |

### Inline-режим

Бот поддерживает inline-запросы:
```
@bot_username 2025-06-15
```
Ответ: список доступных слотов на указанную дату.

## Тестирование

```bash
# Запуск тестов
pytest -v

# С отчётом покрытия
pytest --cov=src --cov-report=term-missing

# HTML-отчёт
pytest --cov=src --cov-report=html
open htmlcov/index.html

# Установка из зафиксированных зависимостей (рекомендуется)
pip install -r requirements-lock.txt
```

## Разработка

```bash
# Линтинг
ruff check src/ tests/

# Форматирование
ruff format src/ tests/

# Проверка типов
mypy src/
```

## База данных

Используется SQLite в WAL-режиме для максимальной надёжности.

**Миграции** применяются автоматически при запуске. Для ручного применения:
```bash
# Через Python
python -c "from src.infrastructure.database.connection import DatabaseManager; db = DatabaseManager('data/manicure_bot.db'); db.initialize_schema()"
```

**Автоматические бэкапы** создаются каждый день в 02:00 (UTC) с ротацией (хранится 7 последних).

## Мониторинг

```bash
# Health check
curl http://localhost:8080/health

# Метрики (если METRICS_TOKEN не задан — только с localhost)
curl -H "Authorization: Bearer your_token" http://localhost:8080/metrics
```

## Архитектура

Проект следует принципам Clean Architecture / DDD:
- **Domain** — чистая бизнес-логика без зависимостей
- **Application** — use-cases и сервисы
- **Infrastructure** — SQLite, HTTP, репозитории
- **Presentation** — Telegram handlers, keyboards

## Известные ограничения

### Только polling-режим
Бот работает исключительно в режиме **long polling**. **Webhook не поддерживается**.

- Поля `WEBHOOK_URL` и `WEBHOOK_PORT` в `.env` присутствуют для обратной совместимости,
  но **полностью игнорируются** при запуске.
- Не устанавливайте `WEBHOOK_URL` в `.env` — бот всё равно запустится в polling.
- Для production-деплоя используйте polling + supervisor/systemd/Docker.

### SQLite
Бот использует SQLite, что означает:
- Только один процесс записи одновременно (WAL-режим смягчает это ограничение)
- Не подходит для высоких нагрузок (>100 одновременных пользователей)
- Для масштабирования потребуется переход на PostgreSQL

### Напоминания
- Напоминания хранятся в памяти (MemoryJobStore) если не задан `jobstore_url`
- При перезапуске бота запланированные напоминания восстанавливаются автоматически
  через `reminder_service.restore_reminders()` из таблицы активных записей

## Лицензия

MIT
