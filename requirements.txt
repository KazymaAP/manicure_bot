# ── Основные зависимости ──────────────────────────────────────────────────────
# ПРОБЛЕМА 14 FIX: смягчены ограничения версий pydantic для устранения конфликта.
# aiogram==3.13.1 требует pydantic>=2.0.3, что несовместимо с жёстким pydantic==2.10.4
# из-за транзитивных зависимостей. Используем диапазон >=2.9.0,<3.
#
# Если нужны строгие версии — установите в два шага:
#   pip install aiogram==3.13.1 pydantic-settings==2.7.0
#   pip install -r requirements.txt
aiogram==3.13.1
# pydantic==2.10.4 вызывает конфликт — используем совместимый диапазон
pydantic>=2.9.0,<3
pydantic-settings==2.7.0
# apscheduler с поддержкой SQLAlchemy jobstore для персистентных задач
apscheduler[sqlalchemy]==3.10.4
python-dotenv==1.0.1
# SQLAlchemy 2.x (совместима с apscheduler[sqlalchemy])
SQLAlchemy==2.0.36
# aiohttp для health-check сервера
aiohttp>=3.9.0
# tzdata для корректной работы ZoneInfo на Linux/Docker без встроенной tz-базы
tzdata==2024.2
# BUG 32 FIX: backports.zoneinfo для Python < 3.9 (используется в reminder_service.py)
backports.zoneinfo; python_version < "3.9"

# ── Тестовые зависимости ─────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-mock==3.14.0

# ── Статический анализ (CI/CD) ───────────────────────────────────────────────
mypy==1.13.0
ruff==0.8.4

# ── Optional: Redis для FSM-хранилища в production ───────────────────────────
# Раскомментируйте строки ниже если используете Redis:
# redis==5.2.1
# aiogram[redis]==3.13.1
