aiogram==3.13.1
pydantic==2.9.2
pydantic-settings==2.6.1
# apscheduler с поддержкой SQLAlchemy jobstore для персистентных задач
apscheduler[sqlalchemy]==3.10.4
python-dotenv==1.0.1
# SQLAlchemy 2.x (совместима с apscheduler[sqlalchemy])
SQLAlchemy==2.0.36
# aiohttp для health-check сервера (без известных уязвимостей)
aiohttp==3.10.11
# tzdata для корректной работы ZoneInfo на Linux/Docker без встроенной tz-базы
tzdata==2024.2

# ── Тестовые зависимости ─────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ── Статический анализ (CI/CD) ───────────────────────────────────────────
mypy==1.11.2
# Stubs для aiohttp (только для mypy, не нужен в runtime)
# types-aiohttp нет на PyPI отдельно — aiohttp 3.x содержит встроенные py.typed стабы
# (пакет types-aiohttp устарел и несовместим с aiohttp>=3.9)

# ── Optional: Redis для FSM-хранилища в production ───────────────────────
# Раскомментируйте строки ниже если используете Redis:
# redis==5.2.0
# aiogram[redis]==3.13.1
